name: Remitly Scraper

on:
  schedule:
    - cron: '0 * * * *'  # tiap jam
  workflow_dispatch:

jobs:
  run-scraper:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run scraper
        run: python remitly_scraper.py
        env:
          BOT_TOKEN: ${{ 7321560024:AAEw0nepmIP6ngV5l8ix_qpe76JLrSeHBoQ }}
          CHAT_ID: ${{ 915455328 }}
